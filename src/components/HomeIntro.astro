<div class="intro">
	<h1 class="intro__heading">Hey there, <br>i’m <span class="intro__accent">Christopher</span>.</h1>
	<div class="intro__copy">
		<p>i’m a senior front-end web developer currently on sabbatical from professional work. i’m recovering from burnout and trying to figure out who i want to be now.</p>
		<p>i use <a href="https://pronouns.org/they-them">they/them pronouns</a>.</p>		
	</div>
</div>

<style>
	h1 {
		font-size: 1.875rem;
		line-height: 1.125;
		font-weight: 500;
		position: relative;
		margin: 0;
		padding-bottom: .25rem;
		margin-bottom: .25rem;
		border-bottom: .125rem solid var(--color-line-L);
	}
	/* h1::after {
		content: '👋🏻';
		display: none;
		position: absolute;
		top: 0;
		right: 0;
		height: .125rem;
		transform-origin: 100% 100%;
	}
	h1:hover::after {
		display: block;
		animation: rotation 1s infinite ease-in-out;
	} */
	@media (min-width: 768px) {
		h1 {
			font-size: 2.5rem;
			padding-bottom: .75rem;
			margin-bottom: .75rem;
		}
	}
	@media (min-width: 1024px) {
		h1 {
			font-size: 3.5rem;
		}
	}
	@media (min-width: 1280px) {
		h1 {
			font-size: 4rem;
		}
	}

	/* @keyframes rotation {
		0% {
			transform: rotate(0deg);
		}
		25% {
			transform: rotate(5deg);
		}
		50% {
			transform: rotate(0deg);
		}
		75% {
			transform: rotate(-5deg);
		}
		100% {
			transform: rotate(0deg);
		}
	} */
	.intro {
		margin-bottom: 4rem;
	}
	.intro__accent,
	.intro__copy a {
		color: var(--color-line-7);
	}
	.intro__copy a {
		font-weight: 500;
	}
	.intro__copy {
		font-size: 1.125rem;
		line-height: 1.5;
	}
	.intro__copy * {
		margin-top: 0;
	}
	.intro__copy * + * {
		margin-top: .5rem;
	}
</style>